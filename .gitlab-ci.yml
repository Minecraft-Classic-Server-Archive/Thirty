stages:
  - build
  - deploy

build debian:
  stage: build
  image: registry.firestick.games/sean/thirty-ci-images/debian:latest
  script:
    - meson setup build
    - cd build && ninja

build alpine:
  stage: build
  image: registry.firestick.games/sean/thirty-ci-images/alpine:latest
  script:
    - meson setup build
    - cd build && ninja

build tumbleweed:
  stage: build
  image: registry.firestick.games/sean/thirty-ci-images/tumbleweed:latest
  script:
    - meson setup build
    - cd build && ninja

scan-build tumbleweed:
  stage: build
  image: registry.firestick.games/sean/thirty-ci-images/tumbleweed:latest
  script:
    - meson setup build
    - ninja -C build scan-build

## Docker image

.kaniko: &kaniko
  stage: deploy
  image:
    name: gcr.io/kaniko-project/executor:v1.22.0-debug
    entrypoint: [""]
  before_script:
    - echo "{\"auths\":{\"${CI_REGISTRY}\":{\"auth\":\"$(printf "%s:%s" "${CI_REGISTRY_USER}" "${CI_REGISTRY_PASSWORD}" | base64 | tr -d '\n')\"},\"$(echo -n $CI_DEPENDENCY_PROXY_SERVER | awk -F[:] '{print $1}')\":{\"auth\":\"$(printf "%s:%s" ${CI_DEPENDENCY_PROXY_USER} "${CI_DEPENDENCY_PROXY_PASSWORD}" | base64 | tr -d '\n')\"}}}" > /kaniko/.docker/config.json

docker:
  <<: *kaniko
  script:
    - /kaniko/executor --context "${CI_PROJECT_DIR}" --dockerfile "Dockerfile"
      --destination "${CI_REGISTRY_IMAGE}:dev"
  rules:
    - if: $CI_COMMIT_TAG
      when: never 
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH

docker tagged:
  <<: *kaniko
  script:
    - /kaniko/executor --context "${CI_PROJECT_DIR}" --dockerfile "Dockerfile"
      --destination "${CI_REGISTRY_IMAGE}:${CI_COMMIT_TAG}"
      --destination "${CI_REGISTRY_IMAGE}:latest"
  rules:
     - if: $CI_COMMIT_TAG
